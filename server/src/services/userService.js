/**
 * Contains business rules related to application users and Supabase identity links.
 * Full application users and Supabase identity links functionality will be implemented in a later phase.
 */
class UserService {
  async getPlaceholder() {
    throw new Error("UserService.getPlaceholder is not implemented.");
  }
}

module.exports = new UserService();
