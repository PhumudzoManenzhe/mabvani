/**
 * Starter component for University Card.
 * Replace the placeholder markup when the component is implemented.
 */
export function createUniversityCard() {
  const element = document.createElement("div");
  element.className = "university-card";
  element.textContent = "University Card placeholder";
  return element;
}
