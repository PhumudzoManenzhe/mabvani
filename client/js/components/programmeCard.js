/**
 * Starter component for Programme Card.
 * Replace the placeholder markup when the component is implemented.
 */
export function createProgrammeCard() {
  const element = document.createElement("div");
  element.className = "programme-card";
  element.textContent = "Programme Card placeholder";
  return element;
}
