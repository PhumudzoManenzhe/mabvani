/**
 * Starter component for Navbar.
 * Replace the placeholder markup when the component is implemented.
 */
export function createNavbar() {
  const element = document.createElement("div");
  element.className = "navbar";
  element.textContent = "Navbar placeholder";
  return element;
}
