/**
 * Starter component for Sidebar.
 * Replace the placeholder markup when the component is implemented.
 */
export function createSidebar() {
  const element = document.createElement("div");
  element.className = "sidebar";
  element.textContent = "Sidebar placeholder";
  return element;
}
