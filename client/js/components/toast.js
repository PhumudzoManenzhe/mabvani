/**
 * Starter component for Toast.
 * Replace the placeholder markup when the component is implemented.
 */
export function createToast() {
  const element = document.createElement("div");
  element.className = "toast";
  element.textContent = "Toast placeholder";
  return element;
}
