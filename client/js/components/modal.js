/**
 * Starter component for Modal.
 * Replace the placeholder markup when the component is implemented.
 */
export function createModal() {
  const element = document.createElement("div");
  element.className = "modal";
  element.textContent = "Modal placeholder";
  return element;
}
