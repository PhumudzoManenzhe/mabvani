/**
 * Starter component for Status Badge.
 * Replace the placeholder markup when the component is implemented.
 */
export function createStatusBadge() {
  const element = document.createElement("div");
  element.className = "status-badge";
  element.textContent = "Status Badge placeholder";
  return element;
}
