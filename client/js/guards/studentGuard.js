/**
 * Will verify student role information from the Render PostgreSQL user record.
 */
export async function requireStudentRole() {
  return {
    allowed: false,
    message: "requireStudentRole has not been implemented yet.",
  };
}
