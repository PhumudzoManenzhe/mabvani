/**
 * Example browser runtime configuration.
 * Copy this pattern into a non-committed local environment file if needed.
 */
window.UNIAPPLY_ENV = {
  SUPABASE_URL: "https://your-project.supabase.co",
  SUPABASE_ANON_KEY: "your_public_anon_key",
  API_BASE_URL: "http://localhost:3000/api",
};

export const ENVIRONMENT_EXAMPLE = window.UNIAPPLY_ENV;
